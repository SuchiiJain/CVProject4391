Create a new Python Enviroment (Venv) to protect the Egg

This is stream of consciousness; Only look towards the bottom

.whl needed for jetpack 6 (apparently some things can't run on our archectecture (ARM/Linux)
pip install http://jetson.webredirect.org/jp6/cu126/+f/5cf/9ed17e35cb752/torch-2.5.0-cp310-cp310-linux_aarch64.whl
pip install http://jetson.webredirect.org/jp6/cu126/+f/5f9/67f920de3953f/torchvision-0.20.0-cp310-cp310-linux_aarch64.whl

We need no dependencies from the ultralytics
pip install --no-deps ultralytics>=8.4.0

Buns-
# https://github.com/PINTO0309/mediapipe-bin
# Numpy version - 3.6.9	(mandatory)
# Python version - 1.19.4

Not Buns but complicated - 
https://github.com/dusty-nv/jetson-containers
1) 14t-pytorch image
2) run installs
  a) pip install --no-deps ultralytics>=8.4.0
  b) pip install mediapipe==0.10.18
3) Get the LSTM running going

Steps to run Data_collection (FOR THE NANO)
1) xhost +
2) git clone https://github.com/SuchiiJain/CVProject4391.git
3) cd CVProject4391/asl_translate
4) git clone https://github.com/dusty-nv/jetson-containers
5) cd jetson-containers
6) bash install.sh
7) cd ..
8) jetson-containers run $(autotag pytorch)
9) pip install --no-deps ultralytics>=8.4.0
10) pip install mediapipe==0.10.18
11) python3 Data_Collection.py
